stanza를 사용하기 위해서는 pytorch가 필요
!conda install -y -c pytorch pytorch torchvision

stanza 설치
!pip install stanza

기본은 kaist이고, package = 'gsd'로 gsd사용
stanza.download('ko')

gensim 설치
!pip install gensim

wordcloud 설치
!conda install -y -c conda-forge wordcloud
